

//TODO when connext supports mainnet
const handleSubscribe = async (lock, currentChain) => { //lock.address, lock.chain

    //Normal purchase
    if (currentChain == lock.chain) {


    } else { //Connext purchase

    }


    //TODO fallback, use Unlock checkout?
}